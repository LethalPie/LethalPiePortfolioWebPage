import { Container } from "react-bootstrap";

const Footer = () => {
  return (
    <Container fluid className="mt-auto text-center">
      <footer>Footer</footer>
    </Container>
  );
};

export default Footer;
